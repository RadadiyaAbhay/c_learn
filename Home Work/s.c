#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
    int a,b,c,d,e,f;
    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);
    scanf("%d", &d);
    scanf("%d", &e);
    //Complete the code to calculate the sum of the five digits on n.
    
    f=a+b+c+d+e;
    printf("%d",f);
    return 0;
}